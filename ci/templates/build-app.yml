parameters:
- name: BUILD_STAGING_DIR
  type: string
  default: '$(System.DefaultWorkingDirectory)/build-staging-directory'

steps:

 - checkout: versioningTemplates
   fetchDepth: 0
 - template: templates/task-setup-execute-git-version.yml@versioningTemplates